function anotherFunChallenge(input){
    let a = 5; // O(1)
    let b = 10; // O(1)
    let c = 30; // O(1)

    for(let i=0; i<input.length; i++){ // O(n)
        let x = i+1; // O(n)
        let y = i+2; // O(n)
        let z = i+3; // O(n)
    }

    for (let p=0; p<input; p++){ // O(n)
        let e = p*2; // O(n)
        let f = p*4; // O(n)
    }

    let myString = "I Love DSA" // O(1)
}

// O(1)+O(1)+O(1)+O(n)+O(n)+O(n)+O(n)+O(n)+O(n)+O(n)+O(1)
// O(4)+O(7) = O(4+7n) = O(n)

anotherFunChallenge() // O(n)
